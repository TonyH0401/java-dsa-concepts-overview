- Graph is a data structure, it include infite graph and finite graph.

- The graph itself also has undirected graph and directed graph.

- These are how the graph is represented:
    - Edge list
    - Adjacency list
    - Adjacency matrix

- We will also gloss over BFS and DFS for Graph